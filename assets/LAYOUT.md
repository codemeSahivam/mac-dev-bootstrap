# Recreate the reference layout in Ghostty:
#
# 1. Open Ghostty (full screen optional)
# 2. Split vertically:  Cmd+D
# 3. Focus LEFT pane → run:  btop
# 4. Focus RIGHT pane → run:  fastfetch  (or open a new tab — fastfetch runs on startup)
#
# Ghostty shortcuts:
#   Cmd+D          split right (vertical)
#   Cmd+Shift+D    split down (horizontal)
#   Cmd+W          close pane
#   Cmd+] / Cmd+[  focus next/prev pane
